import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  fontG: {
    fontSize: 32,
    padding: 20,
    textAlign: 'center',
  },

  fontM: {
    fontSize: 25,
    padding: 20,
    textAlign: 'center',
  },
});
